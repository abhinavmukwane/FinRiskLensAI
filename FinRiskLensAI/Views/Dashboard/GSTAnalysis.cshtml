@model FinRiskLensAI.Core.Models.GST.GSTAnalysisViewModel
@{
    ViewBag.Title = "GST Analysis";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}

@* .fhc-title/.fhc-sub (used for this page's heading) come from here — the
   bank portal's Customer.cshtml already loads it for the same reason. *@
<link href="~/css/FinancialHealthCard.css" rel="stylesheet" />

@* Body is shared with the bank portal Customer 360 — see _GstAnalysisBody. *@
@await Html.PartialAsync("_GstAnalysisBody", Model)
